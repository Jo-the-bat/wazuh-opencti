# Grafana reverse proxy — only functional when monitoring profile is active.
# Uses variable upstream to avoid nginx crash when grafana container isn't running.
server {
    listen 4443 ssl;
    server_name localhost;
    server_tokens off;

    ssl_certificate     /etc/nginx/ssl/opencti.crt;
    ssl_certificate_key /etc/nginx/ssl/opencti.key;

    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers on;
    ssl_session_cache   shared:GRAFANA_SSL:10m;
    ssl_session_timeout 10m;

    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Content-Type-Options "nosniff" always;

    # Use Docker DNS resolver and variable to allow nginx to start even if grafana isn't running
    resolver 127.0.0.11 valid=30s;
    set $grafana_upstream http://grafana:3000;

    location / {
        proxy_pass         $grafana_upstream;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto https;
        proxy_set_header   Upgrade           $http_upgrade;
        proxy_set_header   Connection        "upgrade";
    }
}
